load ../../../code/semantics/interpreter-symbolic.maude
load ../../model/drones/Drones3_rendezvous_Instance#.maude

mod TEST-DRONE3-SYSTEM is
  including DRONES3_RENDEZVOUS_INSTANCE-MODEL-SYMBOLIC .
  including SPECIFICATION-LANGUAGE-SEMANTICS .

  op initConst : -> BoolExp .
  eq initConst = [true] .

  op cinitState : -> Object .
  eq cinitState = initialize(collapse(initial)) .
endm

mod TESTER is
  including TEST-DRONE3-SYSTEM .

  eq @m@ = ['DRONES3_RENDEZVOUS_INSTANCE-MODEL-SYMBOLIC] .
  eq MFlag = TFTF .
endm
